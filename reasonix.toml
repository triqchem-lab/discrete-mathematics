[permissions]
allow = ["Bash(mkdir -p src/Sovereign/Arithmetic/BSD/doc docs/BSD && echo \"done\")", "Bash(cd /data/work/discrete-mathematics && cat > /tmp/test_rewriting.agda << 'EOF'\n{-# OPTIONS --rewriting #-}\nmodule test_rewriting where\nopen import Relation.Binary.PropositionalEquality using (_≡_)\nopen import Data.Nat using (ℕ)\nrecord R1 : Set where; field a : ℕ\nrecord R2 : Set where; field b : ℕ\ne : (R1 ≡ R2) → ℕ; e ()\nEOF\nrm -rf _build && /opt/agda/agda --rewriting /tmp/test_rewriting.agda 2>&1 | grep -c error || echo 0)", "Bash(cd /data/work/discrete-mathematics && echo \"=== 77 模块全列表 ===\" && ls src/Sovereign/Algebra/Jacobian/*.agda | wc -l && echo \"\" && echo \"=== 模块→行数→refl→postulate ===\" && for f in src/Sovereign/Algebra/Jacobian/*.agda; do l=$(wc -l < \"$f\"); r=$(grep -c '= refl' \"$f\" 2>/dev/null || echo 0); p=$(grep -c 'postulate' \"$f\" 2>/dev/null || echo 0); printf \"%-40s %5d %4d %4d\\n\" \"$(basename $f)\" \"$l\" \"$r\" \"$p\"; done | sort -t' ' -k1)", "Bash(cd /data/work/discrete-mathematics && echo \"=== 模块摘要 ===\" && for f in src/Sovereign/Algebra/Jacobian/*.agda; do title=$(head -3 \"$f\" | grep -E \"^-- \\|\" | head -1 | sed 's/^-- | //' | cut -c1-60); printf \"%-38s %s\\n\" \"$(basename $f)\" \"${title:-无标题}\"; done | sort -t' ' -k1)", "Bash(cd /data/work/discrete-mathematics && for f in src/Sovereign/Algebra/Jacobian/*.agda; do base=$(basename $f .agda); echo \"=== $base ===\" && grep \"^open import\" \"$f\" | grep -o \"Sovereign\\.[A-Za-z.]*\" | sort -u; done > /tmp/deps_all.txt 2>/dev/null && wc -l /tmp/deps_all.txt && echo \"---\" && echo \"=== 公共基础设施 (被引用次数) ===\" && cat /tmp/deps_all.txt | grep \"Sovereign\\.\" | sort | uniq -c | sort -rn | head -20)", "Bash(cd /data/work/discrete-mathematics && git add -A && git commit -m \"fix: 批量修复7个预存编译错误\n\n修复文件:\n  CartanTorsion.agda   — postulate...where 反模式分离\n  ParityViolation.agda — tritToℤ 基础定义恢复\n  SpinTwistor.agda     — tritToℤ + postulate...where\n  LossGain.agda        — tritToℤ 引用\n  Boundaries.agda      — tritToℤ 引用\n  Zhonglv.agda         — tritToℤ 引用\n  WuXing.agda          — 缺失定义补充\n\n基础:\n  Trit.agda            — 恢复 tritToℤ: Trit→ℤ {+0,+1,+2}\n\n修复进度: 8/10 (剩余: 7个深层级联文件)\" && git push origin master 2>&1)"]
