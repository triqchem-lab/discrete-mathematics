# 剩余工作·任务清单（2026-08-14 实测版）

> 依据: MSC 2020 分类（/home/yanli/work/xiaomi/glmmath）+ sovereign-analysis-report +
> 2026-08-14 编译器实测（26 子目录, 113 postulate / 35 文件, Problem/ 61 模块全绿）。
> 验证标准统一: **编译 exit=0 + 0 postulate + 文档与代码同步**。

---

## P0 — 诚实性与地基（wiki 三分类校准版）

> **状态 (2026-08-15): P0 收尾完成 — D 类 13 块全部歼灭 (Hilbert6 空目录经用户确认暂缓)。**
> ✅ 轨道 A 假/空 = 0（累计歼灭 15+ 假公理, 含本轮 lossGainPreservesStableRoot/
>   chernConvergence/nayinIsTopological/nayinFingerprintUnique(全称版)/
>   zhonglvNotSunYi/onlyResetMechanism/twelveStepChain/allReachable 空类型公理）。
> ✅ D 类 13 块**全部**歼灭: holonomyLemma (LCM 证明)、SevenStages、Nayin、
>   LengthLattice、LossGain、TQ10、WindingAsymmetry 单调性 132 case +
>   最后一击 Decimal/Proofs (2026-08-15, 路线 a with-free):
>   Axioms.divMod10/sumDigits 对齐 stdlib (n/10, n%10),
>   divMod10Correct ← m≡m%n+[m/n]*n + m%n<n 直证,
>   sumDigitsBound/sumDigitsTerminates/digitalRootConverges/digitalRootStable
>   ← <-wellFounded 良基归纳 + m/n<m 递归, 全程 0 postulate 0 with, 编译绿。
> ✅ 轨道 B 逐文件边界注释批次完成 (20 处)。
> 🟡 剩余: Hilbert6 暂缓（用户确认"不急，不要管"）。

> **校准**（对照 wiki `Postulate审计报告.md` 三分类 + `14-agda-audit` + `23-unformalized-conjectures`）:
> 全库 postulate 非单一"债"。wiki 正式立场: 核心 0 postulate（有范围），外围按三分类管理:
> 可闭合（优先歼灭）/ 物理锚定（genuine bridge，合法公理）/ 机械约束（REWRITE 锁死，保留）。
> **宪法"禁 postulate"的正确读法 = 禁假命题、禁隐藏假设，非"零公理"**（禁公理会杀死物理锚定层）。

### P0-1 三轨处置（替代"清零战役"）

**轨道 A — 假/空 postulate 审计与删除（唯一数学紧急项）**
判据: 命题可被计算反驳，或类型空洞（无数据）。当前已知/已清:
- ✅ 已删（本会话, 全部编译验证）:
  - `HolographicPi`: `k>1-contr`（假, k=2 见证）、`electricCannotExpressHoloPi`（假）、
    `cannotUpgradePi`（改为构造性证明）
  - `ProjectiveOrbit`: `orbit-count-is-4320` + `total-orbits`（假: 4320 非轨道计数,
    Burnside 商 = 4374/54 = 81）; orbit-refl/sym/trans 改为从 ProjectiveCore 重导出
  - `Quantum/Foundation`: `experimental-anchoring : Set`（空洞类型）
  - 效果: 113 → 105 postulate
- ✅ 追加删除（2026-08-15 P0 轮, ⊥ 可导出的假命题, 已编译验证）:
  - `MagicSquare144.windingNotDecomposed`（¬(144≡120+24), refl 反证）
  - `Boundaries.polarIndecomposable`（见证 a=120, b=24）
  - `Boundaries.holoPiIrreducible`（见证 k=2: 144=2·72 ∧ 46=2·23）
  - `DataAnchors.Anchor_EnergyGap_H2O`（断言 867/1000 ≡ 5/10, 数值反驳）
  - 教义（不可拆解/禁约分/≈锚定）保留为注释宪法条款, 不以假命题驻留
- ✅ 已完成（2026-08-15）: falsity 扫描闭合 — Set 空洞 13 处 + ≡ 类 31 处逐一处置,
  叠加 D 类轮再歼 6 个假全称/空类型公理（lossGainPreservesStableRoot、
  chernConvergence、nayinIsTopological、nayinFingerprintUnique(全称版)、
  zhonglvNotSunYi、onlyResetMechanism、twelveStepChain/allReachable）。
  其余全部归入 机械约束/物理锚定/范畴分离宪法条款（见 Constitution/README.md
  登记表, 83 块全表）。**假/空 = 0。**

**轨道 B — 物理锚定类正式登记（honesty upgrade，非 bug）**
- ✅ 已完成（本会话）: 新建 `src/Sovereign/Constitution/PhysicalAssumptions.agda`
  —— 三分类集中登记处（机械约束/物理锚定/可闭合/已清除），逐站点表 +
  边界纪律（"零宣称必须带范围"）。不 import 物理模块（零耦合），grep-able。
- ✅ 已完成（2026-08-15）: 边界注释批次 — 20 处物理锚定站点逐一加
  「轨道 B 物理锚定登记」注释（Zhonglv/Platonics/Aether/XuanwuAbsorption/
  QuartzPhonon/EnergyGap/CartanTorsion/Entanglement, 全部编译绿）;
  M4 eigen16± 已于前轮标注。

**轨道 C — 可闭合类按"优先歼灭目标"排期（质量债，不破不立）**
- ✅ 已完成（本会话）: `Geometry/ProjectiveCore.agda` 的群公理块（g-id-left/right、
  g-inv-left、g-assoc、~g-refl/sym/trans）**全部构造性证明, 0 postulate**。
  关键发现: 旧版 g-mul（直接积）与 g-action-full（C₂ 扭转）不匹配, ~g-trans 为**假**命题
  （反例 ⟨1,0⟩·⟨0,1⟩: x=0 上 2≠1, 数值验证）。修复: g-mul 改为真半直积
  (a,h₁)(b,h₂) = (ψ_{h₂}(a)+₃b, h₁+₂h₂)；新增 action-mul 核心定理（四 flag 组合）
  + 20 个 Fin3/Fin2 引理。效果: 全库 113 → 104 postulate, 假命题清零。
- ✅ 追加完成（2026-08-15 P0 轮）: `MagicSquareM4` — `orthogonal-basis-complete`
  构造性证明（4 显式整数见证）; `manifold-orth-replaces-coprime` 空洞 Set 公理删除;
  `projection-orthogonality` 假公理（任意实例 1·1·1·1≠6624）改为 record 字段
- ✅ 追加完成（2026-08-15 P0 轮）: `Quantum/Foundation.phonon-*` 4 条 → 构造性定义
  （propagate/interference/standing-wave/collapse, 0 postulate）
- ✅ 追加完成（2026-08-15 P0 轮）: `TQ10.polarMod144/toroidalMod46` → refl 证明;
  `CartanTorsion` 4 处未解 hole 闭合（Z₃ 群公理 27+3+3 case refl;
  closeTorsion 挠率矛盾重设计为 ClosedTorsion; cartanLegal hole → 显式 B 登记公理）
- ✅ 追加完成（2026-08-15 P0 收尾轮）: D 类 13 块**全部歼灭** —
  holonomyLemma 构造性证明（m%n≡0⇒n∣m ×2 + lcm-least）;
  LengthLattice 空类型公理删除 + 余数 refl 定理;
  SevenStages 7 case; Nayin 构造版唯一性; TQ10.chernConvergence 假删除;
  WindingAsymmetry 单调性 132 case + 2 假命题删除;
  Decimal/Proofs 最后 5 条全证明闭合（路线 a with-free:
  Axioms 对齐 stdlib div/mod + 良基归纳, 编译绿 0 postulate 0 with）。
- ✅ Decimal/Proofs 闭合细则（2026-08-15）:
  divMod10Correct ← m≡m%n+[m/n]*n + m%n<n;
  sumDigitsBound ← 良基归纳 (rec 隐式 y + m/n<m + q≤q*10);
  sumDigitsTerminates ← ≤-<-trans + lemma_sum_lt + q≥1;
  digitalRootConverges/Stable ← 10 base case + 递归（sumDigitsTerminates 作下降见证）。
- ✅ 追加完成（本会话）: `Geometry/ConformalCore.agda` 8 条 postulate → 0。
  数值穷举确认 conf-mul/conf-action 在**右作用约定**下一致（群论部分为真, 非假）；
  修复两处真缺陷: ① conf-inv 的 s=1 分支（扭曲元素是对合, 真逆元=自身——
  旧版 conf-inv-left 对 s=1,t≠0 为假, 数值反例 t=1）；② t6Inner-conformal
  对平移为假（数值反例 0→2）, 收窄为 t6Inner-scale2-inv + 反例定理
  t6Inner-translation-counter。新增右作用核心定理 action-mul-right。
- ✅ 追加完成（本会话）: `Format/CRT.agda` crtSec-restricted-prop 真定理;
  cubical 桥 2 条 postulate 保留但已实测确认 Agda 2.9.0 跨模块路径限制
  （原注释警告的实证）——归入机械约束类。

### P0-2 空壳处置
- `Problem/Hilbert/Hilbert6/`（空目录）: 立项希尔伯特第六问题离散版（物理公理化，
  锚点: NSRegularity §5 + YM_SpectralGap + PhysicalOntology）或删除空壳。
- ✅ `Jacobian/README.md` 已与实际目录对齐（2026-08-15 P0-4）: 目录树/编译表改
  为真实 15 模块, 6 个不存在的 jac_* 七问题模块行改为指向 Problem/ 实际位置,
  "80 模块"修正为 15 模块。

### P0-3 编译修复
- ✅ `Structology/HolographicSpace.agda`: 加 `--rewriting` 头（2026-08-15）,
  InfectiveImport 消除, `theorem-maximality-4320` 编译绿。
- ✅ 追加（2026-08-15）: `Coupling/CartanTorsion.agda` 预存 4 holes 闭合
  （assoc 27 case / identity / inverse / torsionNonzero 矛盾重设计）, exit 0。
- `Diagnosis/ElectricCivilization.agda` / `AI/Constitution.agda`: stdlib 只读
  文件系统问题——本会话已确认与代码无关（环境），记录即可，不修代码。

### P0-4 文档同步
- ✅ 模块数/等级锚点在 `Algebra/Holographic/FinalAudit.agda`（Problem/ 61 模块,
  8 项 L3）——已对齐; 仓库无独立 INDEX.md, 无需新建。
- ✅ Jacobian README 与实际目录对齐（15 模块清单、七问题行改为 Problem/ 指针）。

---

## P1 — 高价值新定理（延续本会话模式: 命名层 → 真定理）

> **状态 (2026-08-16): 全部完成 (4/4)。** 四项均已编译绿 (0 postulate) 并注册 FinalAudit:
> - P1-1 ✅ `Problem/Riemann/ZetaFunctional.agda` (278 行) — Z(T) 有理式 +
>   函数方程 Z(1/qT)=Z(T) + 零点刚性, 全 refl (ℤ/Eisenstein 系数)。
> - P1-2 ✅ `Coding/BCHGF9.agda` (1257 行) [8,6,3] GF(9) BCH 码: 最小距离≥3、
>   系统编码、单错纠错; `Coding/CyclicGF27.agda` (5030 行) 长度 26 三元循环码:
>   三根定理 + 循环移位封闭 + γ 阶 26。
> - P1-3 ✅ `Analysis/FiniteProbability.agda` (424 行) — Rat 整数对概率、
>   GF3ⁿ-size (3^n 穷举表)、有理期望、Burnside (A₄ 自然作用 1/4 + 正则作用 1/12)、
>   GF(3) 均匀链平稳分布, 全离散无浮点。
> - P1-4 ✅ `RootMath/Eisenstein.agda` (967 行) — 环公理 8 条
>   (+ᵉ-comm/assoc/identity/inverse, *ᵉ-comm/assoc/identity/distrib)、
>   conjᵉ-mul 共轭同态、norm-form/norm-mul 范数乘性、6 单位 + unitGen C₆ 循环。
> - 注: `*ᵉ-assoc` 规范型 8 项通过 +0 包裹三步法 (wrap → *-assoc → unwrap) 规避
>   ℤ 变量乘法的 sign ◃ 归一化卡点; 已注册 FinalAudit。

### P1-1 有限域 zeta 完整化（11M 离散版）
- 位置: `Problem/Riemann/ZetaFunctional.agda`（新模块）
- 内容: Z(T) = (1−αT)(1−βT)/((1−T)(1−qT)) 有理式；函数方程 Z(1/qT) = Z(T)；
  零点 = α⁻¹,β⁻¹ 的 Eisenstein 刚性（|α|=√q 的代数形式 = t²+3s²=4q 已证）。
- 验证: 三条曲线全通 + refl。✅ 完成 (2026-08-16)

### P1-2 GF(9)/GF(27) 纠错码（94B）
- 位置: `Coding/BCHGF9.agda` + `Coding/CyclicGF27.agda`（新模块）
- 内容: GF(9) 上的 BCH 码最小距离界；`GF27Separation` 的根结构 → 3 元循环码
  （γ 的本原性已证, 直接可用）；编码/解码正确性。
- 验证: 0 postulate, 编译绿。✅ 完成 (2026-08-16)

### P1-3 有限概率空间（60A 离散版）
- 位置: `Analysis/FiniteProbability.agda`（新模块）
- 内容: GF(3)ⁿ 均匀分布、期望（Rat 整数对, 宪法兼容）、Burnside 概率
  （随机 A₄ 作用不动概率 = 1/12 类定理）、Markov 链在有限环上的平稳分布。
- 验证: 0 postulate, 编译绿。✅ 完成 (2026-08-16)

### P1-4 Eisenstein 环公理收口（12F/11R）
- 位置: `RootMath/Eisenstein.agda`
- 内容: `*ᵉ-assoc`、`+ᵉ-comm/assoc`、逆元（ℤ[ω] 的 6 个单位）、
  N(xy)=N(x)N(y)（范数乘性, 用 WeilRigidity 的范数模式）。
- 验证: 环公理 8 条 + 范数乘性, 0 postulate, 编译绿。✅ 完成 (2026-08-16)

---

## P2 — 扩展现有强项

### P2-1 YM 谱定理扩展
- `Problem/YangMills/YM_SpectralGap.agda` → 2×2 Wilson 圈完整谱（全部 4 类
  构型） + 一般 A,B 参数的间隙公式 Δ=3B 的符号证明。

### P2-2 HoTT 真 A₄ 纤维化
- 7-08 自评 Step 4 遗留: TotalSpace = T6Lattice, 投影 x↦[x], 纤维 = 真 A₄ 轨道
  （T6.agda 已有真 A₄ 作用）→ 非平凡局部平凡化, 替换空洞的 totalSpaceEquiv。

### P2-3 IhC60Vibration 深化
- 特征标正交性全表（100 项内积 = δ 验证）；选择定则（IR 活性 = T₁u, Raman = A_g/H_g）；
- 与 `Physics/H2OC60.agda` 锚定行对接（46 从"定义"变为"引用本定理"）。

### P2-4 量子真不可克隆
- `Quantum/Foundation.agda` 5 条 postulate 清零后: 线性 + 酉性 ⟹ 克隆映射不存在
  （qutrit 版本, 参照 P4 报告的目标）；测量坍缩的构造性定义。

---

## 附录: 本会话已完成（2026-08-14）

| 项 | 状态 |
|---|---|
| HolographicPi 原子化（3 假 postulate 清除, ⊥ 不可导出） | ✅ |
| SpiralCycle ⟨2⟩ 六环 + 142857 | ✅ 新建 |
| WeilRigidity（Weil 界 + L(E,1)=#E + Eisenstein 共轭对） | ✅ 新建 |
| YM_SpectralGap（精确谱 + Δ=3） | ✅ 新建 |
| NSRegularity §5（动力学四定理） | ✅ 扩展 |
| IhC60Vibration（I_h → 46, 100 项特征标表） | ✅ 新建 |
| GF27Separation（根集 = Frobenius 三元组, 27 穷举） | ✅ 新建 |
| FinalAudit + 重构报告同步（8 项 L3） | ✅ |
| Nayin root9 / LossGain hiding 修复 | ✅ |
