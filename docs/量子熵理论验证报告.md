# 渠玉芝量子熵（熵旋）理论的库内验证报告（2026-08）

> 验证方式：以已完成的 0-postulate 库（`EntropySpinLaw.agda` 等）为基准，
> 对公理文档 ESB-MASS-EMERGENCE-20260306 中熵旋定律的结构断言逐条验证。
> 结论口径：**结构断言可验证/可修正；数值预言不背书。**

## 验证矩阵

| # | 定律断言 | 库内验证 | 结论 |
|---|---|---|---|
| V1 | 熵旋密度张量反对称 ρ_S^{μν} = −ρ_S^{νμ} | `EntropySpinVerification.v-rho-antisym`（S 场逐点，符号） | ✅ **成立** |
| V2 | m=∮S·dA 的旋度项消失（离散斯托克斯） | `v-stokes`（= `massIntegral-curl-zero`）+ `entropySpinMass-reduction`（m ≡ −Σ[(κ·ℋ²)·1]） | ✅ **成立**（质量积分只余 ℋ² 项） |
| V3 | ∇·S = 0 熵旋守恒 | 新证 `divℚ-curl-zero` + `divS-identity`：divℚ S ≡ −∂z((κ·ℋ²)·1) | ⚠️ **修正**：无条件守恒不成立；守恒 ⟺ ∂z((κ·ℋ²)·1)=0（如 ℋ² 沿 z 常值），`divS-zero-condition` 落链条件形式 |
| V4 | 量子化 ∮S·dA = C·m₀ | 对照已证 m ≡ −Σ[(κ·ℋ²)·1] | ❌ **不可从已证部分推出**：需要额外假设（m₀ 定义、陈数量子化、Σℋ² ∝ m₀ 匹配）——诚实边界 |
| V5 | 光超导阈值（a≥6 仲吕闭合 → 热辐射） | `EntropySpin.thermal-a6`（a=6 闭合实例） | ✅ **闭合实例成立**；任意 a 的通用通道为注释级 |

## V3 的数学内容（本轮的实质新证）

1. **ℚ 场代数**（新引理，全部符号证明）：差分可加性（dx/dy/dz-add）、负号穿越
   （neg）、减法分配（sub）、三对混合偏导交换（dxdy/dydz/dzdx-commℚ）、
   六项重排 six-rotateℚ、成对抵消 pair-cancel-1/2/3ℚ。
2. **divℚ∘curlℚ = 0**：旋度部分的散度恒为零（`divℚ-curl-zero`）——
   熵旋守恒的"旋度内核"成立。
3. **divS-identity**：divℚ S = −∂z((κ·ℋ²)·1) —— 守恒的完整条件形式。
   公理文档的"∮_{∂M} S·dA = 0 保证 ∇·S = 0"是**过度断言**：闭合流形条件
   只消旋度项，ℋ² 的 z 向变化贡献 −∂z 项。
4. `law-eq`：`entropySpinLaw`（vscale 形式）与 `S-field`（显式分量形式）
   逐点等价（s·0ℚ ≡ 0ℚ 步）。

## 与已验证库的接缝

- 复用 `EntropySpinLaw` 的全部 ℚ 场定义与求和引理（sum3ℚ/telescope/merge4）；
- 与 GF(3) EM 五层（`DiscreteEMCore.div-curl-zero` 等）同构的证明模式：
  域律符号链 + 构造子实例化（无 funext）；
- `negneg`（ℚ 双负）为 stdlib 2.4 未导出的本地三情形引理。

## 微观输运证明层（EntropySpinMicro，2026-08 补全）

"缺乏微观数学证明"的缺口在本层闭合，四个可证核心：

| 定理 | 内容 | 意义 |
|---|---|---|
| `flux-diff` | fluxK(S)(k+1) − fluxK(S)(k) ≡ −(heatK(k+1) − heatK(k)) | **通量输运恒等式**：熵旋流沿 z 的变化完全由 ℋ² 项决定——旋度部分（纯熵旋态）**无损耗**，"光超导低耗散通道"的微观数学证明 |
| `flux-conserved` | heatK 沿 z 守恒 ⟹ fluxK 沿 z 守恒 | 无损通道的守恒条件（闭合形式） |
| `thermal-general` | ∀ a ≥ 1，m(a,2) < 1/1000（交叉相乘 2·268² < 10⁵·(a+1)） | **修正 a≥6 边界**：公式层的跨界点实际在 a≥1，公理文档的 a≥6 分界不是公式的推论 |
| `canonical-mass` | 单点 ℋ²=1 构型 m ≡ −((κ·1)·1) | 量子化闭合实例；一般 {0,1} 场 m ≡ −κ·N·1 需 9 点布尔分支+分配律，开放项（诚实边界） |


## 诚实边界

1. κ=0.85 与 0.0268 均为经验常数，验证不涉及它们的推导；
2. 质量预言表（轻子/夸克/玻色子质量谱）与本报告的 V1–V5 无关，其内部
   不自洽已在《量子熵理论选型-规范决定.md》附录 A 记录，不背书；
3. V4 的量子化断言是定律与库的**开放接缝**：若提供 m₀ 与陈数量子化的
   公理化，即可在此继续落链。
